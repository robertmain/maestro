const config = {
    http: {
        port: 3000,
        bind_address: '0.0.0.0'
    },
    adapters: {
        "disk": {
            "songs_directory": "C:\\mysongs"
        }
    }
};

export default config;
